0.1
- add an ASCII progress bar to visualize the rendering process
-> new dependency: progressbar package (PyPI)
-> setup.py, README
- fix all important TODO and FIXME comments in the ``*.py`` files!
- CLI
- rename the argument ``list`` to ``list-projects``
- add the argument ``show-status`` which shows the files to be rendered
- test all logging calls and implement every prepared test
